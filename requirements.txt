# --- custom MCP server (runs standalone, no agent deps needed) ---
mcp>=1.9.0
pydantic>=2.7
python-dateutil>=2.9
icalendar>=6.0
beautifulsoup4>=4.12
lxml>=5.2

# --- agent side ---
langgraph>=0.2.60
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-mcp-adapters>=0.1.0
python-dotenv>=1.0

# --- dev ---
pytest>=8.0
ruff>=0.6
